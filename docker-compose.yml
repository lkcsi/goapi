version: '3.3'
services:
  books_api:
    build: .
    env_file:
      - .env
    networks:
      - bookstore
    volumes:
      - ./:/app
    ports:
      - "8081:8081"
  books_db:
    build: ./book-database
    networks:
      - bookstore
    environment:
      MYSQL_ROOT_PASSWORD: asdfgh
    ports:
      - "3306:3306"

networks:
  bookstore:
